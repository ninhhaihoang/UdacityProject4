import { checkForName } from "../src/fe/js/nameChecker"

describe("Test check input url", () => {
    test("test fucntion checkForName() is working", () => {
        expect(checkForName).toBeDefined();
})});